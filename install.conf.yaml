- clean:
    - "~"
- shell:
  - [git submodule update --init --recursive vim/bundle/, Installing plugins]
- link:
    ~/.vim:
      force: true
      path: 'vim'
    ~/.tmux:
      force: true
      path: 'tmux'
    ~/.tmux.conf:
      force: true
      path: 'tmux/tmux.conf'
    ~/.tmux.conf.local:
      force: true
      path: 'tmux/tmux.conf.local'
    ~/.bashrc:
      force: true
      path: 'bash/bashrc'
    ~/.vimrc:
      force: true
      path: 'vim/vimrc'
    ~/.config/fish:
      create: true
      path: 'fish'
    ~/.gitconfig:
      force: true
      path: 'git/gitconfig'
    ~/.gitignore:
      force: true
      path: 'git/gitignore'
    ~/.bin:
      force: true
      path: 'bin'

